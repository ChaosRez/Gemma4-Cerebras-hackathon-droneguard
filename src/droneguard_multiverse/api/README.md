# API

The backend API should stay lightweight.

Required endpoints:

- list scenarios
- get scenario details
- start a run in live or replay mode
- get run trace events
- get cached agent response details

Avoid adding a database, queue, or complex workflow engine for the hackathon MVP.

